export const metadata = { title: 'KAMA Power', description: 'Batteriespeicher & Solarenergie' }
export default function RootLayout({ children }) {
  return <html lang="de"><body>{children}</body></html>
}
